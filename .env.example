# ==========================================
# IPTV Admin Portal - Environment Configuration
# ==========================================
# Copy this file to .env and update with your actual values
# ==========================================

# Server Configuration
PORT=3000
NODE_ENV=production

# MySQL Database Configuration
DB_HOST=localhost
DB_USER=your_database_username
DB_PASSWORD=your_database_password
DB_NAME=iptv_admin
DB_PORT=3306

# Default Admin Credentials (First Time Setup)
ADMIN_USERNAME=admin
ADMIN_PASSWORD=admin123

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d

# 360Messenger WhatsApp API Configuration
WHATSAPP_API_KEY=your_360messenger_api_key_here
WHATSAPP_API_URL=https://api.360messenger.com/v2/sendMessage

# Alert Configuration
ALERT_DAYS_BEFORE_EXPIRY=7

# Cron Schedule (for cPanel cron job)
# Run every 10 minutes: */10 * * * *
CRON_SECRET=your-secure-cron-secret-key-change-this

# Company Information (for Invoices)
COMPANY_NAME=IPTV Solutions
COMPANY_ADDRESS=123 Business Street, City, Country
COMPANY_PHONE=+1234567890
COMPANY_EMAIL=info@iptv-solutions.com
COMPANY_WEBSITE=https://www.iptv-solutions.com
COMPANY_LOGO_URL=

# Currency Settings
CURRENCY=USD
CURRENCY_SYMBOL=$
TAX_RATE=0

# Invoice Settings
INVOICE_PREFIX=INV

# Auto-generate Credentials
AUTO_GENERATE_CREDENTIALS=true
PASSWORD_LENGTH=10

# File Upload (if needed)
MAX_FILE_SIZE=5242880
UPLOAD_PATH=./uploads

# CORS Settings (Frontend URL)
FRONTEND_URL=http://localhost:3000

# ==========================================
# cPanel Deployment Notes:
# ==========================================
# 1. Create a MySQL database in cPanel
# 2. Update DB_* values with your cPanel MySQL details
# 3. Set up Node.js application in cPanel (select Node version 14+)
# 4. Set application root to the project directory
# 5. Set application startup file to server.js
# 6. Add all environment variables in cPanel Node.js app settings
# 7. Install dependencies: npm install
# 8. Import schema.sql into your MySQL database via phpMyAdmin
# 9. Set up cron job in cPanel:
#    Command: curl -X POST https://yourdomain.com/api/cron/check-expiry -H "Authorization: Bearer YOUR_CRON_SECRET"
#    Schedule: */10 * * * * (every 10 minutes)
# 10. Start the application
# ==========================================
